---
enabled: true
imageRenderer:
  implementation: "ContactSheetImageRenderer"
  allowedGroups: "public"
  maxWidth: 3000
  maxHeight: 3000
  keepAspectRatio: true
directoryImageRenderer:
  implementation: "ContactSheetDirectoryRenderer"
  allowedGroups: "public"
  maxWidth: 3000
  maxHeight: 3000
  maxImages: 20
output:
  format: "AUTO"
  quality: 92
